# Postgres connection (Neon: use the pooled connection string)
DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require

# JWT signing secret — set a long random value in production
APP_SECRET=change-me
